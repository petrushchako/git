name: Custom-Runner Test

on:
#   push:
#     branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: [self-hosted, Linux, mbc]
    steps:
        - run: echo "Hello World"
        - run: echo $HOSTNAME
